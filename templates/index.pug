extends layout
include mixins/panel
include mixins/header

block content
  .page
    .hero
      +masthead(masthead)
      p.hero-msg= heroMsg
      if notFoundMsg
        p.rv= notFoundMsg
      +zipForm(null)
      p(aria-hidden="true").cursor= readyMsg
